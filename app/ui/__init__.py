"""UI assets placeholder.

The full UI (React + Vite) lives in a separate repository. This
package exists so that the FastAPI app can mount a static-files
directory at ``/ui`` without crashing when the assets aren't
present in a production deployment.
"""