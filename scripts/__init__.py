"""Helper scripts for development and operations.

Each submodule is a runnable script (see ``if __name__ == '__main__'``
blocks). They are NOT deployed in production — only the Docker image
runs them as one-shot setup tasks during build.
"""